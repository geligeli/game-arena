package(default_visibility = ["//visibility:public"])

# The Dockerfile arena_tournament builds a problem's sandbox image from. Data of
# the tool, not built here: bazel does not drive docker, and the image is a
# property of a problem repo, not of the arena.
exports_files(["Dockerfile"])
