package(default_visibility = ["//visibility:public"])

# The Dockerfile arena_tournament builds a problem's three images from: the
# sandbox, a participant's kit, and the tournament itself. Data of the tool,
# not built here: bazel does not drive docker, and each image is a property of
# a problem repo or of one participant, not of the arena.
exports_files(["Dockerfile"])
