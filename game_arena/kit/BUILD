package(default_visibility = ["//visibility:public"])

# The Dockerfile arena_tournament builds a participant's kit image from. Data
# of the tool, not built here: bazel does not drive docker, and the image is a
# property of one participant's kit, not of the arena.
exports_files(["Dockerfile"])
