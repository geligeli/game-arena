# Generated by the arena at submit time.
# Do not edit: it is part of the submission's patch.

load("@rules_cc//cc:cc_binary.bzl", "cc_binary")
load("@rules_cc//cc:cc_library.bzl", "cc_library")

package(default_visibility = ["//visibility:private"])

cc_library(
    name = "strategy",
    hdrs = [
        "strategy.h",
    ],
    deps = [
        "//bots:bot_api",
    ],
)

cc_binary(
    name = "bot",
    srcs = ["//bots:bot_main.cc"],
    local_defines = [
        r'CANDIDATE_ENTRY_HEADER=\"' + package_name() + r'/strategy.h\"',
    ],
    deps = [
        ":strategy",
        "//bots:bot_api",
        "//bots:bot_deps",
    ],
)

# Not the arena's: what makes this directory part of a kit. It is the
# participant everyone starts as, and deliberately not in the sandbox's tree.
filegroup(
    name = "kit",
    srcs = glob(["*"]),
    visibility = ["//visibility:public"],
)
